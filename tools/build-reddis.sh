: <<-note
This script installs a Reddis database specifically for use with Harbor and is expected to reside in the same namespace. It is not to be called as a standalone executable since it has specific dependencies on variables and functions from the main build script.
note

## TODO
